<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"> <!-- Adjust the margin value as needed -->

    <!-- Image at the top (2/3 height) -->
    <ImageView
        android:id="@+id/recipeImage"
        android:layout_width="wrap_content"
        android:layout_height="200dp"
        android:layout_weight="2"
        android:scaleType="centerCrop"
        android:src="@drawable/sample_food" />

    <!-- Recipe Name -->
    <TextView
        android:id="@+id/recipeName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/recipeImage"
        android:layout_marginTop="8dp"
        android:padding="8dp"
        android:text="Recipe Name"
        android:textSize="18sp"
        android:textStyle="bold" />

    <!-- Horizontal Line -->
    <View
        android:id="@+id/horizontalLine"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@+id/recipeName"
        android:layout_marginTop="8dp"
        android:background="@android:color/darker_gray" />

    <!-- Calories and Prep Time -->
    <TextView
        android:id="@+id/caloriesAndPrepTime"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/horizontalLine"
        android:layout_marginTop="8dp"
        android:padding="8dp"
        android:text="280 kcal / 10 mins"
        android:textSize="14sp" />

</RelativeLayout>


